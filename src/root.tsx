import React from 'react';
import { BrowserRouter as Router, Switch } from 'react-router-dom';

const Root = () => {
  return (
    <Router>
      <div>
        <Switch>
        </Switch>
      </div>
    </Router>
  );
};

export default Root;
